# Development configuration file.

include "production.conf"

# Development overrides
session.timeout: 15 minutes
http.cacheStaticResources: false
browserLoggingLevel: debug

ensureSSL: false

db.default.url: "jdbc:sqlite:/tmp/foo.db"
